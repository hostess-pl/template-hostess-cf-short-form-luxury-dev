---
interface Props {
  text: string;
}

const { text } = Astro.props;
---

<span class="inline-flex items-center justify-center rounded-lg border border-border/70 bg-card px-3 py-1.5 text-center text-[0.7rem] font-medium leading-snug tracking-wide text-foreground sm:text-xs">
  {text}
</span>
